# v0.14.0 · 语言包兼容与多语言试听（2026-09-26）

- **客户端语言切换**：兼容英文字符串表头前的注释及 AUDIOFILE 音频键；单个字幕文件异常只影响该文件，并提供具体诊断，不再阻断初始化。无需建立完整索引即可正常浏览卡牌和按需试听。
- **语言联动与记忆**：筛选器与资源语言默认同步，可在设置关闭；关闭后，筛选器按页面记忆，资源语言跨卡牌与重启独立记忆。
- **多语言对照**：资源语言旁新增“显示其他已安装语言”，默认关闭并记忆状态。主语言优先显示，其他语言按对应音频和触发条件嵌入同一语音组件，在分隔线下显示略小的台词及试听、重播和导出按钮；支持统一折叠／展开，默认展开，共用主列表分页。
- **加载响应**：额外语言逐种加载，不自动批量解码或识别；试听、导出、导航优先，切卡或关闭后丢弃旧结果。
- **版本展示**：标题栏、左上角及设置页统一显示工具版本，修正设置页停留在 0.4.0 的问题。

## 升级

退出旧程序，完整解压新版并保留 workspace。保留原索引、收藏、WAV 缓存与导出；
语音关系查询缓存自动更新，按需读取即可，无需为修复英文启动问题全量扫描。
语言包下载完成后重新连接炉石目录。

## 多语言行为

主语言使用原有客户端字幕、公开台词与识别流程。其他语言只加载客户端台词和
声音引用，缺失字幕显示“暂无台词”，不会以主语言冒充。其他语言可逐条试听、
重播、导出，也支持已开启的配套音效和合并导出选项。
搜索、语音分类和事件分组以主语言为基准；其他语言跟随对应主行，使用同一分页。统一折叠/展开默认展开，展开无需再次解析资源。

实现与故障定位见 LANGUAGES.md。

## 本次验证

- 193 项 Python 回归及 18 项 Node 回归通过，JavaScript 语法检查通过。
- 本机 WebView2、Qt 兼容引擎通过真实客户端语言验收：未建立完整索引启动，中英字幕与双向联动、独立记忆、行内对应、统一折叠/展开及翻页、额外语言试听与逐条导出、切卡取消。
- 内置中英文 Vosk 模型实际加载及静音识别通过。
- 新构建便携 EXE 已连接独立 QA 工作区并显示卡牌，正常关闭后端；包内前端文件与源码哈希一致。
- 以上为当前 Windows 本机验证，未进行全新 Windows 虚拟机验收。
