# 本机验收记录

日期：2026-09-21。游戏目录：`F:\Games\Hearthstone`。
可执行文件产品版本：`36.6.3.55344`。索引快照：`164e170a2a7138320324`。

## 0.2 本轮新增验收

- 16 项自动化测试通过；JavaScript 两个入口均通过语法检查。
- 真实 Qt 旧功能回归 9 个阶段通过，新增功能 13 个阶段通过，台词专项 2 个阶段通过。
- 新筛选索引：3,075 条英雄/皮肤记录，其中非酒馆敌人筛选返回 1,356 条；双职业测试 `SCH_350` 同时出现在法师与潜行者结果。
- 组合筛选“法师、0 费、法术”返回 112 条本地记录（含衍生/内部记录）。
- 完整卡面实际获取 512×776 PNG；本地火车王原画 512×512，导出文件字节不变；目录和 PNG 文件包含“火车王里诺艾”。
- 配套试听实际播放火车王登场语音与 1 条登场曲，暂停同步生效；勾选写入设置。
- 英文实际音频包为缺失，语言切换显示提示；未安装的外语无法完成真实播放验证。
- 加尔鲁什 43 个语音片段中 36 条有本地中文台词；实际页面验证首屏显示“我选择死亡！”、“嗬，你好。”；火车王 3 条语音无本地文本，明确显示未附台词。
- 1060×720 窗口无横向溢出；声音分类下拉框完整位于视口内。
- 独立空工作区首次弹出向导、连接成功保存目录、重启后无向导，均通过。
- 所有测试窗口关闭后解析子进程均退出。

证据：`.cache/qa-workbench/results.json`、`.cache/qa-first-run/results.json`、`.cache/qa-transcripts/results.json`，截图位于相同目录。验证使用源码入口；未重新生成或验收 PyInstaller 分发包。

## 0.1 历史验证（保留供参考）

- 读取全部 5,072 个 Data/Win AssetBundle，以及 65 个 Unity Player 内置资源文件，共 5,137 个文件；索引失败 0 个。
- 36,024 条合并卡牌记录、2,198 条英雄 / 皮肤记录。
- 71,883 个 AudioClip、155,577 个可定位 GameObject、69,781 个纹理资源、16 个视频资源。GameObject 数量不是视觉特效数量，视频目前没有专门播放界面。
- 单元 / 回归测试 11 项通过：中文回退、GUID、Windows 文件名、分类、真实 WAV / 多子采样、导出路径、查询、收藏、索引续扫与损坏更新、短事务、表情缓存键。
- 六个分类分别抽取 2 条音频，12 条均完成 PCM WAV 解码及标准库 `wave` 检查。
- Qt 实际页面验证：卡牌列表、火车王普通原画、7 个金卡图层项、10 个异画图层项、加尔鲁什关联语音、音频 `readyState=4` 且实际处于播放状态、WAV 导出、火球术粒子预览、音频库分页。
- 火球术引用图识别 14 个 ParticleSystem、5 个关联音频；画面使用本地粒子纹理，明确显示实验性二维预览说明。
- 交付 BAT 已直接启动测试；UTF-8 + CRLF 行尾，关闭窗口后解析后端退出。

## 皮肤补测

| 卡牌 ID | 本地名称 | 关联声音条目 | 说明 |
| --- | --- | ---: | --- |
| HERO_01 | 加尔鲁什·地狱咆哮 | 56 | 无解析错误；保留主题 1 原画 / 金卡 |
| HERO_01a | 麦格尼·铜须 | 62 | 两个 phone 视觉预制体 GUID 不在本机清单中，界面显示缺失原因 |
| HERO_01bn | 死亡之翼 | 198 | 商城预制体超过 4,000 对象上限，关联结果为部分结果并明确标注 |
| EX1_116 | 火车王里诺艾 | 7 | 无关联解析错误；普通、金卡、异画可提取 |

不同皮肤的音频 ID 集合已经检查，未错误合并成同一英雄的一套音频。关联条目数可能包括相同声音在多个事件下的引用；导出时按音频 ID 去重。

## 证据与复现

- `.cache/qa/results.json`、`.cache/qa/*.png`：真实桌面操作及截图。
- `.cache/audio-check.json`：12 条抽样音频结果。
- `.cache/local-validation.json`：本地索引状态与皮肤补测。
- `.cache/index-final-summary.txt`：全量扫描后状态。
- `tools/gui_smoke.py`：Qt 页面自动验收，末尾在后端忙碌时关闭窗口并检查子进程状态。
- `tools/verify_local.py`：卡牌与皮肤关系检查。

这些本机证据不随源码仓库提交。没有逐一播放 71,883 条声音，也没有逐一验证所有卡牌的素材完整性；没有完成原版 Unity 特效还原或独立 EXE 的无 Python 环境测试。

## 2026-09-22 交互修复回归

- 离线单元测试：26 项通过；Python 编译检查和三个前端 JavaScript 文件的语法检查通过。
- 新增真实 Qt GUI 验证：11 个阶段通过，报告与截图位于 `.cache/qa-fixes/`。
- 既有 `gui_workbench.py` 全部阶段通过；`gui_transcripts.py` 两个阶段通过，HERO_01 仍有 36 条本地字幕。
- 翻页前后页面位置均为 600 px；48 项追加后得到 96 个不重复条目。
- 火车王三条公开台词匹配到唯一基础事件，来源来自真实 API；两个语音各自保留导出入口。
- 加速测试主音轨自然结束后，配套音轨仍继续播放并能暂停；20 秒测试音轨在约 14.99 秒处按上限截停。
- 三粒子系统真实特效样本的画布像素检查通过；最终独立进程首次请求约 1.79 秒，期间列表请求约 105 毫秒。取消后旧结果不重新打开侧栏。
- 测试窗口退出后普通解析子进程已停止；宿主也显式清理独立特效进程。

范围、台词覆盖与二维预览限制见 [本轮说明](INTERACTION_FIXES.md)。未声称已穷举所有资源、鼠标驱动、在线资料页和冷缓存组合。

## 2026-09-22 台词来源、重试与命名更新

- 最终离线自动化测试 **55 项通过**，Python 编译与前端 JavaScript 语法检查通过；覆盖旧工作区来源迁移、禁用开关、音频键及歧义分支、Wiki.gg 卡牌 ID、空缓存重试、离线成功缓存回退、已有本地字幕保护。
- 真实网络使用当前 `fetch_source`：hsdata 返回 17,744 个精确键；Wiki.gg 火车王页面有效但无台词；百度、灰机各返回三条台词。没有声称 hsdata 收录完整随从字幕。
- `tools/gui_transcript_recovery.py` 四阶段通过：失败后重新启用按钮、重复点击只请求一次、部分恢复与来源标签、切卡丢弃旧响应和关闭联网时隐藏入口、四个内置源顺序与不可编辑规则。字幕接口采用固定测试响应，其余使用真实 Qt 与本地卡牌。HERO_01 仍显示 36 条本地字幕，窗口退出后后端已结束。
- 截图与报告：`.cache/qa-recovery/`；通过查看截图确认重试入口、来源标签和卡牌布局可读。
- `tools/verify_local.py` 完成真实四卡回归，保留不同皮肤声音及节日外观。麦格尼缺少的两个手机资源与死亡之翼超出既有 4,000 节点限制仍明确报告，不将不完整资源记为成功。
- 包名、入口、导入、前端调试对象、日志、打包配置、文档与 `.gitignore` 改为砰砰解析台的 `pengpeng` / `PengPengWorkbench` 命名。历史工作区日志及第三方依赖内容不改写。
- 未执行完整 PyInstaller 发行包构建；源码桌面入口已通过真实启动测试。

## 2026-09-22 轻量语音识别与来源默认值

- 离线自动化测试 **62 项通过**，Python 编译检查通过。旧测试只更新来源默认顺序/开关的预期，保留匹配、重试和来源隔离覆盖；新增缓存、禁用、多子采样、路径/时长边界、超时、闲置释放和下载路径检查。
- `tools/speech_smoke.py`：4 条真实中文语音、独立进程识别、内容缓存及闲置回收通过。2.3–3.9 秒音频推理约 0.91–1.28 秒，首条连同启动约 1.89 秒；峰值工作集约 260 MiB。详细结果在 `.cache/qa-speech/benchmark.json`。
- `tools/gui_speech.py`：真实 Qt **6 阶段通过**，覆盖来源默认值、等待公开来源结束后仅识别缺失项、自动识别及明确标记、逐条强制重试、准确字幕优先、开关持久化、取消及过期结果隔离。来源优先级阶段使用固定延迟响应，其余识别由真实中文小模型执行。
- `tools/gui_transcript_recovery.py`：原有 **4 阶段通过**，HERO_01 仍显示 36 条本地字幕；网络失败、恢复、切卡隔离和新默认排序符合预期。
- 已查看设置与语音界面截图；修正识别结果未计入文字统计的歧义，分别显示来源台词与语音识别数量。GUI 截图、报告位于 `.cache/qa-speech-gui/` 和 `.cache/qa-recovery/`；测试恢复原设置，确认普通解析和识别子进程退出。
- 未验证全库识别准确率。火车王等夸张声线存在明显误识别，识别仅作缺失台词参考；本机没有英语游戏语音包，未做英语真实游戏语音测试，未构建独立发行包。方案、用法和性能边界见 [语音识别说明](SPEECH_RECOGNITION.md)。
